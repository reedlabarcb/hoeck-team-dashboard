openpyxl==3.1.2
pdfplumber==0.11.10
# Word (.docx) text extraction — scripts/python/office_extract_text.py.
# Package name is `python-docx`; the import name is `docx`.
# MUST stay in lockstep with the pip line in nixpacks.toml (missing it there builds
# fine and then throws at runtime when the TS worker spawns the subprocess).
python-docx==1.1.2
# Test-only: fpdf2 generates fixture PDFs at test time. Not used in runtime code.
# Pinned to a recent version with stable Page/Cell APIs.
fpdf2==2.7.9
